---
- name: Deploy snapraid-arr-sync wrapper script
  ansible.builtin.template:
    src: snapraid-arr-sync.sh.j2
    dest: /opt/snapraid-arr-sync.sh
    owner: root
    group: root
    mode: "0755"
  tags: [bootstrap, snapraid]
