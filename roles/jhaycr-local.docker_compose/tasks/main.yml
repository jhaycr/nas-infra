---
- name: Process Docker configurations for each host group
  include_tasks: "1_process_group.yml"
  loop: "{{ group_names }}"
  loop_control:
    loop_var: inventory_group_name

- name: Docker operations
  ansible.builtin.include_tasks:
    file: 2_docker_operations.yml