# .env

ZITADEL_MASTERKEY='{{ secret_zitadel_masterkey }}'
ZITADEL_DB_PASSWORD='{{ secret_zitadel_db_password }}'
ZITADEL_DB_ADMIN_PASSWORD='{{ secret_zitadel_db_admin_password }}'
ZITADEL_ADMIN_USERNAME='{{ secret_zitadel_first_instance_org_human_username }}'
ZITADEL_ADMIN_PASSWORD='{{ secret_zitadel_first_instance_org_human_password }}'
