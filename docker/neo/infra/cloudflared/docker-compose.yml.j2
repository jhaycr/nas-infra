
services:

  cloudflared:
    container_name: cloudflared
    image: cloudflare/cloudflared:latest
    command: tunnel --no-autoupdate run --token {{ secret_cloudflare_tunnel_token }}
    networks:
      - default
      - bridge_proxy_external
