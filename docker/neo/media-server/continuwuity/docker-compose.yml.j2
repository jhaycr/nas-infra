---
services:

  continuwuity:
    container_name: continuwuity
    # Calendar versioning (v26.x) since v26.6.0 - the old v0.5.x series is
    # no longer maintained. Skip v26.6.0/v26.6.1, which upstream flagged for
    # disruptive bugs; v26.6.2 is the fix release.
    image: forgejo.ellis.link/continuwuation/continuwuity:v26.6.2
    # Upstream ships /sbin/conduwuit as the binary name (continuwuity forked
    # from conduwuit and kept the entrypoint name); the base image's default
    # entrypoint doesn't invoke it, so it must be set explicitly here.
    command: /sbin/conduwuit
    ports:
      - "8014:8008"
    volumes:
      - {{ docker_appdata_path }}/continuwuity:/var/lib/continuwuity
    env_file:
      - .env
