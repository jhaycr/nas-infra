---
services:

 vaultwarden:
  image: vaultwarden/server:1.35.2-alpine
  container_name: vaultwarden
  restart: unless-stopped
  environment:
    DOMAIN: "https://vw.lab.{{ secret_domain }}"
  volumes:
    - {{ docker_appdata_path }}/vaultwarden/:/data/
  ports:
    - 8087:80