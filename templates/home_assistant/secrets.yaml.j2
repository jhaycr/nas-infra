# Rendered to /config/secrets.yaml on oracle by bellackn.hass_control
# (set mode only). Values come from group_vars/oracle/vault.yml — never
# hardcode here. NOTE: this REPLACES the live secrets.yaml wholesale; every
# !secret name referenced by the HA YAML config must have an entry here.

# Plex server token — used by the rest_command.plex_get den-TV glue in
# configuration.yaml. Same value as secret_arr_api_key_plex in neo's vault
# (deliberately duplicated: each host's play only loads its own vault).
plex_token: "{{ secret_plex_token }}"
